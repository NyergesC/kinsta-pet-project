type Query {
    trips: [Trip!]!
    trip(id:ID!): Trip
}

type Trip {
    id: ID!,
    destination: String!,
    description: String!,
    distance: String!,
    duration: String!,
    src: String!,
    alt: String!,
  }